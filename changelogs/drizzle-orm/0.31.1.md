# New Features

## 🎉 Expo + Drizzle Live Queries

#### `useLiveQuery` hook added to expo-sqlite driver

```tsx
import { useLiveQuery, drizzle } from 'drizzle-orm/expo-sqlite';
import { int, sqliteTable, text } from 'drizzle-orm/sqlite-core';
import { openDatabaseSync } from 'expo-sqlite/next';
import { Text } from 'react-native';

const expo = openDatabaseSync('db.db');
const db = drizzle(expo);

const users = sqliteTable('users', {
  id: int('id').primaryKey({ autoIncrement: true }),
  name: text('name'),
});

const App = () => {
  // * Will automatically re-render when the data in the table changes
  const { data, error, updatedAt } = useLiveQuery(db.select().from(users));

  // const { data, error, updatedAt } = useLiveQuery(db.query.users.findFirst());
  // const { data, error, updatedAt } = useLiveQuery(db.query.users.findMany());

  return <Text>{JSON.stringify(data)}</Text>;
};

export default App;
```
