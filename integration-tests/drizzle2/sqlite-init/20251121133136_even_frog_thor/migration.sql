CREATE TABLE `users12` (
	`id` integer PRIMARY KEY,
	`name` text NOT NULL,
	`email` text NOT NULL
);
--> statement-breakpoint
CREATE TABLE `users13` (
	`id` integer PRIMARY KEY,
	`name` text NOT NULL,
	`email` text NOT NULL
);
