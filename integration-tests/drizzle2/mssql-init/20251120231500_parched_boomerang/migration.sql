CREATE TABLE [users12] (
	[id] int IDENTITY(1, 1),
	[name] text NOT NULL,
	[email] text NOT NULL,
	CONSTRAINT [users12_pkey] PRIMARY KEY([id])
);
--> statement-breakpoint
CREATE TABLE [users13] (
	[id] int IDENTITY(1, 1),
	[name] text NOT NULL,
	[email] text NOT NULL,
	CONSTRAINT [users13_pkey] PRIMARY KEY([id])
);
