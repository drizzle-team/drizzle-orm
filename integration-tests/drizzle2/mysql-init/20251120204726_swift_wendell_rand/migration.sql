CREATE TABLE `users12` (
	`id` serial PRIMARY KEY,
	`name` text NOT NULL,
	`email` text NOT NULL
);
--> statement-breakpoint
CREATE TABLE `users13` (
	`id` serial PRIMARY KEY,
	`name` text NOT NULL,
	`email` text NOT NULL
);
