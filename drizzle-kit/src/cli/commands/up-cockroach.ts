export const upCockroachDbHandler = (out: string) => {
	// const { snapshots } = prepareOutFolder(out, "cockroachdb");
	// const report = validateWithReport(snapshots, "cockroachdb");

	console.log("Everything's fine 🐶🔥");
};
